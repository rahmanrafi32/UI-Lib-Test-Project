import { styled } from "@material-ui/core";

const table = () => {
    const MainDiv = styled("div")({
        padding: "0 30px",
      });
    return (
        <MainDiv>
            <h1>Showing table</h1>
        </MainDiv>
    );
};

export default table;